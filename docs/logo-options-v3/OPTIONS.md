# Logo options v3 (brace-shield + S)

Round 3 DNA from the ChatGPT mark Michell liked: **brace-shield + flat S**. Live favicon / Pages untouched.

1. **Flat brace-S** (`01-flat-brace-s.svg`) — solid teal braces + flat light S on a dark plate.
2. **Filled seal** (`02-filled-seal.svg`) — teal disc; white braces + S punched.
3. **Closed badge** (`03-closed-badge.svg`) — braces merge into a continuous shield outline + flat S.
4. **Heavy line** (`04-heavy-line.svg`) — single-weight stroke braces + S (light on dark; invert for dark on light).
5. **Brace + seal notch** (`05-brace-seal-notch.svg`) — flat brace-S with a small closed notch at the mid join.

Palette: accent/fill `#0d9488`; light `#f8fafc`; plate `#0f172a`. No blue gradient; no Microsoft TS glyph. Braces thickened for 16–32px. Soft bias: flat / filled seal / closed badge stay chunkiest (heavy line + notch mush first at tiny sizes).

Each option ships SVG + 32px PNG (`*-32.png`).
