{
  "name": "superset-ts",
  "private": true,
  "version": "0.3.2",
  "description": "Checked entry into brands and validated shapes. brand type, validate type, and cast<> expand to plain TypeScript: nominal types stock tsc enforces, plus runtime companions. Not a TypeScript fork.",
  "license": "MIT",
  "author": "Michell Ayala",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mishelashala/superset-ts.git"
  },
  "homepage": "https://mishelashala.github.io/superset-ts/",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm --filter @mishelashala/superset-ts-core test",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
