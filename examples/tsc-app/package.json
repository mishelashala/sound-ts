{
  "name": "tsc-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "sts build",
    "dev": "sts watch",
    "test": "node --test test/fail-closed.test.mjs"
  },
  "devDependencies": {
    "@mishelashala/sound-ts-cli": "workspace:*"
  }
}
