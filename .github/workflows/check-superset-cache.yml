name: superset-cache

on:
  push:
  pull_request:

jobs:
  untracked-cache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail if .superset is tracked
        run: bash scripts/check-superset-untracked.sh
