name: sound-ts-cache

on:
  push:
  pull_request:

jobs:
  untracked-cache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail if .sound-ts is tracked
        run: bash scripts/check-sound-ts-untracked.sh
